# =============================================================================
# SalesPro API Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env files to version control

# =============================================================================
# Database Configuration
# =============================================================================
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/salespro_dev

# =============================================================================
# Server Configuration
# =============================================================================
PORT=4000
NODE_ENV=development

# =============================================================================
# Session Configuration
# =============================================================================
# Required in production - generate with: openssl rand -base64 32
SESSION_SECRET=your-session-secret-here

# =============================================================================
# AWS SES Email Configuration
# =============================================================================
# AWS region for SES (e.g., us-east-1, eu-west-1)
AWS_REGION=us-east-1

# Verified sender email address in SES
# Must be verified in your SES account or in a verified domain
SES_FROM_EMAIL=noreply@leaptodigital.com

# Frontend application URL (used for password reset links)
APP_URL=http://localhost:5173

# =============================================================================
# AWS Credentials (set via export, not in this file)
# =============================================================================
# AWS credentials should be set using export commands before starting the server:
#   export AWS_ACCESS_KEY_ID=your-access-key-id
#   export AWS_SECRET_ACCESS_KEY=your-secret-access-key
#
# The AWS SDK automatically picks up these environment variables.
# This approach keeps credentials out of config files and works with:
# - IAM roles when deployed to AWS
# - AWS credential profiles
# - Other AWS credential sources

# =============================================================================
# Legacy MongoDB (ETL Migration)
# =============================================================================
# Connection string for the legacy Parse/MongoDB database
# Used for ETL migration of data from the old system
# If connected to a replica set, reads will automatically use secondary nodes
# LEGACY_MONGODB_URI=mongodb://user:password@host:27017/parse_db
